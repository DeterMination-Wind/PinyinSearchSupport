name: Release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android build-tools
        run: sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: Download Mindustry desktop jar
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .ci
          curl -L --fail \
            -o .ci/Mindustry.jar \
            https://github.com/Anuken/Mindustry/releases/download/v159/Mindustry.jar

      - name: Build merged artifacts
        shell: bash
        run: |
          set -euo pipefail
          rm -rf dist
          gradle_cmd="gradle"
          if [ -f ./gradlew ]; then
            chmod +x ./gradlew
            gradle_cmd="./gradlew"
          fi
          $gradle_cmd --no-daemon clean deploy -PmindustryJar="$PWD/.ci/Mindustry.jar"

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          body_path: RELEASE_NOTES.md
          files: |
            dist/*.jar
            dist/*.zip
